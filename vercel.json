{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && next build && node scripts/copy-pdfkit-fonts.js",
  "crons": [
    {
      "path": "/api/cron/check-outstanding-invoices",
      "schedule": "0 9 * * *"
    }
  ]
}
